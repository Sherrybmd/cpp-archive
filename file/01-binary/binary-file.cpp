#include <iostream>
#include <fstream>
#include <cstring>
UNFINISHED UNFINISHED UNFINISHED
using namespace std;

void saveClass(Student &student) {
    ofstream outfile("student_class.bin", ios::binary);
    if (outfile.is_open()) {
        outfile.write(reinterpret_cast<type>()
    }
}

int main() {

    cout << "\n";
    return 0;
}
